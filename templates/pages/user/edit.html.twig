{% extends "base.html.twig" %}

{% block body %}
    <div class="container text-center bg-secondary px-5 py-5 my-5">
        
        <div>
            <h1 class="text-white mt-4">Mofification d'utilisateur</h1>
        
        {{ form_start(form) }}
            <div class="form-group">
                {{ form_label(form.Username )}}
                {{ form_widget(form.Username )}}
                <div>
                    {{ form_errors(form.Username) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.Adress )}}
                {{ form_widget(form.Adress )}}
                <div>
                    {{ form_errors(form.Adress) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_label(form.Email )}}
                {{ form_widget(form.Email )}}
                <div>
                    {{ form_errors(form.Email) }}
                </div>
            </div>
            <div class="form-group">
                {{ form_row(form.submit) }}
            </div>
        </div>
        <a href="{{ path('app_user') }}" class="btn btn-success w-50" >Revenir à la liste d'utilisateur</a>
        {{ form_end(form) }}
    </div>
{% endblock %}
